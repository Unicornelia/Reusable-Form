import styled from "@emotion/styled";

export const Button = styled("button")(() => ({
  padding: "10px 24px",
  fontSize: "16px",
  fontWeight: 600,
  borderRadius: "4px",
  backgroundColor: "lightblue",
  cursor: "pointer"
}));